import * as React from 'react';
import { Avatar } from 'react-native-paper';

const Avatar = () => (
  <Avatar.Image size={24} source={require('../assets/background.png')} />
);

export default Avatar